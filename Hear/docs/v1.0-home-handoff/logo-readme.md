# HEAR logo — locked master

Geometry is frozen.

Exactly four mark elements:
1. dot aligned over H
2. small ellipse aligned over E
3. medium ellipse aligned over A
4. large ellipse aligned over R

Do not redraw, regenerate, respacing, or reinterpret the logo.

Use:
- `hear-logo-on-light.svg` on light backgrounds
- `hear-logo-on-dark.svg` on dark backgrounds
- `hear-mark-*` for symbol-only uses

`Sound opens worlds.` is NOT part of the logo asset.
Render the claim as live UI text.

Light/dark variants differ only in dot/wordmark contrast.
Ellipse geometry and gradients are identical.
