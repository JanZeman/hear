# [What agent-base should change - short verb phrase]

**Status**: New
**Opened**: <!-- TODO: YYYY-MM-DD -->
**Agent Base version**: <!-- TODO: the value in AGENT_BASE_VERSION when this was observed -->
**Blocks**: <!-- TODO: the work here that is waiting on this, or "-" if nothing is -->

<!-- Status values are exactly these seven, nothing else:
     New | Collected | Accepted | Refused | Downstream-only | Released | Resumed
     This project sets New, and sets Resumed once it has actually gone back to the work named in
     Blocks. Agent-base writes everything in between, together with the decision block below; do
     not edit that block by hand.

     Blocks is this project's field, outside the agent-base-owned block, so agent-base never
     rewrites it. Name a roadmap item where one exists, otherwise one sentence saying what was
     suspended and exactly where it stood. "-" is correct for a request that blocks nothing; such
     a request goes straight from Released to Resumed with nothing to return to. -->

## Observation

<!-- TODO: What was seen, stated as an observation rather than a proposed solution. Name this
     repository's real files, paths, and specifics; that is the evidence agent-base needs, and
     agent-base is responsible for genericizing whatever it adopts. -->

## Evidence

<!-- TODO: A reproduction, a file and line, a command and its output, or the number of times it
     recurred. Never record a secret value. -->

## Why this is generic

<!-- TODO: Could a wholly unrelated repository adopt this change unchanged and benefit? Answer it
     honestly, including "probably not, but the underlying problem may be". Agent-base applies the
     same test and will disagree out loud if it has to; a request that admits its own limits is
     more useful than one that oversells. -->

<!-- AGENT-BASE-DECISION:START -->
## Decision

_Not yet triaged._

<!-- Owned by agent-base. It writes the disposition, the date, the roadmap item ID when one was
     created, and the released version when the change ships. Everything outside this block and
     the Status line stays byte-for-byte unchanged, which is what lets an automated audit tell a
     legitimate decision writeback from an accidental edit to the request itself. -->
<!-- AGENT-BASE-DECISION:END -->
